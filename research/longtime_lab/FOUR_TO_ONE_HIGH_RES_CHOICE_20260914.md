# 4:1 高分辨率起点选择（2026-09-14）

本机已验签的最长原始 H8 旧二次峰值 C 主轨道止于 canonical `τ=14.20020125954303`、step 19135、`961×321`；目前没有找到 `τ=15` 的可信 checkpoint。新的 `(2,0)` 外区密度 C、均摊密度 v2 是独立的原始 `t=0` 轨道，不能拿旧 C 末态直接以新 C 续算。4:1 高分辨率版因而从新的物理 `t=0` 启动。

在旧 C `τ=14.200201` 和新 C v2 `τ=3.700010` 两份验签接受态上，用正式 `hierarchicalAxisPairs` 对两个候选 4:1 *起点节点数*进行了同箱、同密度参数的纯几何反事实测试。原轨道未改，未进行原生跨策略场迁移或时间推进。

| 4:1 根网格 | 初始节点 | 旧 C τ14.2：X/Y 最大相邻比、X 核心 | 新 C τ3.7：X/Y 最大相邻比、X 核心 |
| --- | ---: | --- | --- |
| `n=256`，1025×257 | 263425 | `1.046834/1.055349`，36.833 格 | `1.032696/1.040519`，45.218 格 |
| `n=320`，1281×321 | 411201 | `1.035636/1.042217`，46.687 格 | `1.024539/1.030730`，47.161 格 |

两者均由正式初选阶段提出 3 个候选；Y 核心预测均为 36.800 格。`n=320` 使旧极端态的 X 核心额外留出约 9.85 格，最大网格比从 1.055349 降至 1.042217，因此作为高分辨率 `main.m` 默认值。该选择需要约比 `n=256` 多 56% 的初始二维节点；实际稀疏 LU 内存和单步耗时尚未测量。

对原始解析 `t=0` 数据执行与生产路径相同的 `plannedAxisPairs`、真实初值采样、核心/前沿观察及注册参考轴质量门的无 LU 预检：`n=256` 有 3 候选、首候选核心 X/Y `47.713/62.881`；`n=320` 有 3 候选、首候选核心 `38.500/78.805`，前沿 `30.664`，8 个资源登记级别均构造通过。默认总节点预算 `1,700,000`，登记包含 `1281×321→2561×641` 的双向二倍级别（1,641,601 节点），但预算只限定程序，不能保证服务器的内存足够。

证据 JSON 为 `result/verification/four_to_one_20260914/geometry_v1.json` 与 `t0_n{256,320}_v1.json`，研究脚本为 `compare_four_to_one_frozen.m` 与 `preflight_four_to_one_initial.m`。冻结几何和无 LU 预检不证明高分辨率原生初值、LU、长时重网格、Profile 空间收敛或边界无影响；需在目标服务器上做独立短程资源试跑后，再持续监测 `main.m` 启动的新轨道。
