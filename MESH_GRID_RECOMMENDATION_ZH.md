# 长时间 Profile 的自动网格选择与已知问题

2026-09-13 补充：原始 `t=0` 的 version-4 H8 长跑已自动越过旧 τ≈11.67 的候选族容量边界。
在 τ11.7004 的冻结场，常规 X/Y 合格试探为 0/2，一级分层 X 搜索仍提出 3 个候选；
主轨道随后在 τ11.932175 自动选择 X 编号 82、Y 编号 4，真实迁移通过。τ12.00025
已验签状态为 641×321、累计 35 次重布、X/Y 核心 30.780/36.208、格宽比
1.072844/1.057942。连续 X 峰值观察揭示离散峰值列的竖向衰减率估计偏大，但隔离
影子测试尚未看到请求分歧，因此正式门槛保持不变。问题与证据汇总见
[自动网格研究说明](research/longtime_lab/AUTONOMOUS_GRID_DESIGN_FROM_FROZEN_DATA_20260913.md)。
同一冻结场的预先时间窗估算曾提示第三候选可能延长重布间隔。该冻结场的
X89 已通过真实场迁移审计，但实际触发时刻的第三候选变成 X90，因此不能
把冻结场排序直接沿用到请求时刻。在真实 τ11.932175 请求处，X82 与 X90
均通过真实场迁移及安全审计；隔离 X90 分支的下一次请求发生于 τ12.171527，
原主轨道为 τ12.083839，间隔从 0.151663 增至 0.239351（+57.8%），最大
相邻比从 1.072844 增至 1.075200。该单次反事实续跑未写正式 checkpoint，
尚不能证明长期最优或 Profile 精度，详见下方研究说明及证据。
后续第37、38次请求的留出排序均保留原候选1；跨节点级改善瞬时格宽比
时还需计入更多节点的计算成本，见[均摊寿命留出检验](research/longtime_lab/AMORTIZED_MESH_LIFETIME_HOLDOUT_20260913.md)。

本说明依据 H8、原始物理 `t=0` 轨道在 canonical 时间 `τ=7.400881253551652`、step 3903 的可信接受步冻结场。来源网格为 `641×321`，初始网格为 `321×161`。表中的数值是**该冻结场的无 LU 几何候选**，不是新网格上继续演化的误差界，也不是 H8 计算箱充分大的证明。

## 节点成本与最差相邻比

以初始两轴的整数单元增幅为登记级别，在 `maximumTotalNodes=1000000` 下共登记 38 级，其中 19 级从该来源网格可单调增长。按节点数递增扫描合格候选后，最大相邻格宽比 `max(r_x,r_y)` 的严格改进前沿如下：

| 单元因子 X×Y | 节点 X×Y | 总节点 | `r_x` | `r_y` | `max(r_x,r_y)` |
|---|---:|---:|---:|---:|---:|
| 2×2 | 641×321 | 205761 | 1.052818 | 1.041136 | 1.052818 |
| 3×2 | 961×321 | 308481 | 1.029980 | 1.041136 | 1.041136 |
| 3×3 | 961×481 | 462241 | 1.029973 | 1.025271 | 1.029973 |
| 4×3 | 1281×481 | 616161 | 1.020468 | 1.025271 | 1.025271 |
| 4×4 | 1281×641 | 821121 | 1.020468 | 1.017946 | 1.020468 |

如果预算允许约 46.2 万节点，`961×481` 是当前冻结态较均衡的 X/Y 方案；若成本优先，`961×321` 用约 30.8 万节点将最差比降到 1.041136。单独把 X 加到 `1281×321` 仍由 Y 限制，最差比不比 `961×321` 更好。这里的“最优”只指已登记、通过质量门的候选中的最小最大相邻比；没有断言所有可能非均匀网格的全局最优。

同一真实轨道在更晚的可信 `τ=9.300546671954852` 冻结态，严格改进前沿的节点配置仍是上表五组，最差比分别变为 `1.062837`、`1.048762`、`1.036248`、`1.030233`、`1.025002`。这表明瓶颈方向的增点顺序在两时刻稳定，但格宽比随演化恶化；自动策略必须根据当下场重新规划，不能把任一冻结态网格固定到整个长跑。详细数值和复现入口见 `research/longtime_lab/V5_LATE_FROZEN_PARETO_20260913.md`（研究工作树）。

独立冻结场前向/回转评分对 `961×481` 给出相对峰跳 `7.638e-5`、质量守恒缺损 `3.535e-15`、值域越界 `1.921e-9`，均通过研究评分门；这仍不是生产原生 `mesh.build` 与后续 PDE 的验收结果。

## 自动化规则

服务器的 `autonomousMeshVersion=5` 从初始节点数和**启动前**指定的总节点上限自动登记方向级别，无需在时间推进中编辑网格。每轴整数因子 1–6 全覆盖，再按约 4/3 的因子增长到上限。触发重布后，按二维节点成本递增尝试注册级别；在每级内最小化 `max(r_x,r_y)`，质量裕量及稳定索引打破并列。网格必须保持同一计算箱、X 对称、精确 `x=±1` 锚点、正求积、局部权重与模板条件，并在真实原生迁移后再验收核心/前沿分辨率、峰跳、质量和值域。失败候选不会改变接受态。相同参考轴的几何试探只在一次来源规划中缓存，冻结态四组逐值等价检查通过。

`maximumAdjacentGridRatio=2` 仅使最大相邻格宽比在两轴都不超过带舍入容差的 2 时不触发网格平滑停机。其他安全门、时间/步数终点与节点预算仍独立生效。`maximumTotalNodes` 是节点上限，不保证稀疏 LU 的内存可承受。先用短程资源试跑确认服务器容量，再从新的 `t=0` 长跑启动；checkpoint 不能中途改这个上限。

## 造成过早停机的已知网格问题

1. 旧 H8 轨道到约 `τ=11.6731` 时，固定方向/参数候选族耗尽。基础 70 个 X 候选全部被拒，但 Y 仍有可用候选；这不能推出该物理状态没有可行网格。
2. 约 `τ=4` 的冻结态，`321×161` 网格 X 候选不足，增加 Y 节点几乎无益；先增加 X 节点可明显扩大几何可行集。
3. 到约 `τ=7.4`，`641×321` 的 X/Y 两轴都影响最差相邻比。只加 X 到 `961×321` 后 Y 成为瓶颈；再加 Y 到 `961×481` 才继续降低最差比。这要求方向级别可自动组合，而非始终同步翻倍。
4. `H8` 的远边界源/速度指标已超过 `0.01` 告警阈值。该长跑可检验自动网格与时间推进，但最终 Profile 还需要更大计算箱、空间/时间误差和独立初始网格比较。网格比改善本身不证明接近无穷时间极限或强奇异性。
5. H256/Y128 用固定 `321×161` 从 `t=0` 启动时，解析观测后的 X 候选数为零，程序正确报告初始容量拒绝；在相同质量门与 310000 总节点上限内，启动前自动筛选选择 `401×161`，有 3 个解析候选，原生 `t=0` 短程、checkpoint 和续算到 τ=.008 均通过。初始节点数也必须随计算箱自适应，不能只在演化过程中增长。

H64/Y32 和 H128/Y64 在同一 `τ=0.004` 的第五版原生短程结果中，`c_l` 分别为 `0.481804510743`、`0.485157205859`，物理梯度分别为 `0.834895396`、`0.835261875`。远边界源比从 `0.065838` 降到 `0.0379519`，但速度比从 `0.838378` 升到 `0.852326`；单一速度告警不是域收敛误差估计。两箱节点轴并不相同，因此这些差异也不能单独归因于截断误差，仍需匹配物理时间、内核解析度和时间步的配对比较。

现有验证包括真实冻结态 38 级规划、独立冻结场迁移评分、原生第五版 `t=0` 短程与 checkpoint 续算，以及 H8 至 τ=0.5 的 5 次自然重布；321×161→641×161 的原生直接迁移、安全审计和 flow 也已通过。完整 `all quick` 与新旧物理核等价检查通过。第五版晚期大节点原生迁移、完整长跑和大箱 Profile 仍是待做的数值实验。
