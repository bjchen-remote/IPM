# R2.6 自动网格层级搜索修复候选包（2026-09-14）

R2.5 的新 C、`meshDensityVersion=2` 原始 t=0 轨道在 canonical `τ=2.0649288466`、step 1227 停于 `autonomous_mesh_error`。末态场和网格仍有限，原生检查点完整；第 1228 步 Y 核心刚降至 31.9986 格、触发补充网格搜索时，程序发现搜索候选身份账本不一致而回退。原因是 R2.4/R2.5 的均摊密度初选圆滑参数改为 `[8,16,24,32,40]` 后，第一补充阶段只有 230 个新元组，冻结的搜索契约却要求 260 个。这是程序错误，不能把 R2.5 描述成已经能稳定长跑的版本。

R2.6 只把两种均摊密度的**补充阶段**圆滑参数上界延伸至 44，使阶段计数重新等于登记的 `70/260/170`。初选 70 个候选、Y/X 密度函数、同节点设计余量、C 规范、节点预算以及 1.08 相邻比和全部真实迁移验收门保持不变；旧密度模式的搜索序列不变。公开接口回归现在逐项核对两种密度的实际阶段数。对原签名检查点重放第 1228 步，规划成功；在独立目录进行原生续算至 `τ=2.2`，step `1227→1323`、自然重布 `13→14`，正常因设定的 `final_time` 结束并保存检查点。

新增事务的原生审计通过：真实 X/Y 核心 `41.026/36.800`、前沿 `24.185`、最大相邻比 `1.068315/1.038745`、相对峰跳 `1.369e-7`，末态检查点重新验签通过。完整快速回归与旧数值等价套件均通过。

R2.5 目录与 ZIP 不修改，用于复现该停机。旧策略或旧 C 检查点不能切换密度版本；相同冻结 `densityVersion=2` 的 R2.5 检查点经隔离验证可用 R2.6 修复源码继续，但正式长跑应使用独立目录与新检查点前缀。R2.6 只越过首次故障点，尚未证明自动 REMESH 能连续跑到强奇异性，也未证明无穷时间 Profile 的时空/箱长收敛。增加网格前应先看修复后的长跑及真实停机前缀。

服务器使用见 [README_SERVER_ZH.md](README_SERVER_ZH.md)，完整错误堆栈、验证和限制见[层级搜索修复记录](research/longtime_lab/BALANCED_SEARCH_STAGE_REPAIR_20260914.md)。R2.5 的冻结态核心余量实验见[原发行说明](RELEASE_NOTES_R2_5_CORE_RESERVE_ZH.md)。
