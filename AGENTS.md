# 本副本的维护约定

本文件只适用于 `ipm_grid_v6/`。用户要求保留此前求解器；除非另有明确要求，
不要修改相邻的原版目录，不把本副本的运行依赖指向原版。

先读 [STRUCTURE.md](STRUCTURE.md) 和拟修改模块的 `INTERNAL.md`。
历史材料位于 [research](research/README.md)，其中的旧路径、计划与操作指令不是本副本的维护要求。

- 保留唯一求解入口 `ipm.solve`，配置每次运行只解析一次。
- 不为目录整齐而增加类、转发包装或第二套数值路径；辅助函数按实际职责归属。
- 数值模块不调用研究脚本或测试代码；`ipm.verify` 是验证调度的独立入口。
- 保持 `ny × nx` 场布局、冻结分组配置、v2 结果、网格/快照配对及重网格验收后提交的契约。
- 结构调整不顺带改变公式、默认值、数值选择或浮点运算顺序；数值改动需单独说明并验证。
- 新旧一致性验证可读取原版，不在原版目录写日志或产物。
- 快速检查使用短小显式配置，关闭保存、绘图及视频；不运行无参数生产算例代替检查。
- 根据改动范围运行 [验证套件](tests/README.md)，共享数值路径改动须包括新旧一致性验证。
- 在 [CHANGELOG.md](CHANGELOG.md) 记录实际执行结果与限制，不把未运行或历史测试写成通过。
- 生成产物归入 `result/`，保留已有结果；历史原文不要因函数更名而批量重写。

文档保持简洁：总览只写边界与公共契约，内部细节就近记录，历史推导留在研究目录。
