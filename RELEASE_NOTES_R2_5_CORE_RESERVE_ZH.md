# R2.5 同节点核心余量候选包（2026-09-14）

**后续实跑更正：**该包从 t=0 到 `τ=2.0649288466` 首次补充搜索时因候选身份计数错误停于 `autonomous_mesh_error`；不能用于声称长时自动稳定。修复与隔离续算证据见 [R2.6 说明](RELEASE_NOTES_R2_6_SEARCH_REPAIR_ZH.md)。原 R2.5 ZIP 保留不改。

这一候选包默认 `meshDensityVersion=2`：沿用 R2.4 把 Y 外区相邻格宽增长近乎均摊的密度函数，把 X 的注册设计缓冲 `xPadding` 从 1.10 提到 1.50。X 仍只搜索 70 个候选，初始节点数与 310000 节点预算不变；全轴最大相邻比 1.08、曲率 0.01、核心/前沿、真实迁移与流场有限性门均未放松。`0` 保留旧密度，`1` 保留 R2.4 密度；旧检查点的冻结配置不得切换为 `2`。

验签冻结态的同节点原生迁移给出：新 C `τ=3.500136` 的 641×321 候选最大比 X/Y=`1.050595/1.026118`，实际核心 `46.456/36.158`；更极端的旧 C `τ=14.200201` 在 961×321 上最大比 `1.059386/1.042217`，实际核心 `47.767/36.942`。两者都通过原生迁移审计与后续流场检查。相邻旧态 `τ=14.100094` 也通过，X 核心 `45.288`。旧 C 的结果是将新密度几何应用于旧策略允许的节点级作反事实迁移，不是旧断点的新版本续算。

正式新模式已从原始物理 `t=0` 自动运行到 canonical `τ=0.5`：304 步、6 次自然重布、正常按设定终点结束；末态 321×161，核心 X/Y≈`43.442/36.230`，相邻比 X/Y≈`1.049928/1.043198`。这验证了自动路径能使用新策略，但**尚未验证**从零持续到 `τ=14`、强奇异性或无穷时间 Profile 的网格收敛。下一步应在独立运行目录进行长程检查点算例，检查每次自然 REMESH 的实际余量，再决定增加节点和箱长。

另从原始 `t=0` 到 `τ=.25` 保存验签原生 checkpoint，随后同策略续算到 `.30`：步数 `158→189`、自然重布 `1→2`，密度版本 2 保持冻结。
公开接口测试、完整 `ipm.verify('all','quick')` 与旧数值等价快速套件均已通过。

服务器启动与续算见 [README_SERVER_ZH.md](README_SERVER_ZH.md)。密度扫描、相邻比负担分布、原生迁移和版本边界见 [极端冻结态试验](research/longtime_lab/EXTREME_FROZEN_MESH_RESERVE_20260914.md)。现有 R2.2/R2.3/R2.4 发布包与运行中轨道没有被改写。
