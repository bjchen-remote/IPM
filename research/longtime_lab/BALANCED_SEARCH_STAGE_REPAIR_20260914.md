# 均摊密度的层级搜索阶段修复（2026-09-14）

R2.5 `meshDensityVersion=2` 的原始 `t=0` 轨道从验签 `τ=.30` 继续后，在 canonical `τ=2.0649288466033111`、step 1227 安全停于 `autonomous_mesh_error`，末态 checkpoint 已保存，原 R2.5 包与旧 R2.2 运行未被修改。停止前网格为 `321×161`、已自主重布 13 次，X/Y 核心 `35.917592/32.00179`，最大相邻比 `1.0739662/1.0718093`，安全因子约 `0.2132`。这是**搜索证据契约错误**，并非场迁移质量拒绝或网格资源上限耗尽；不能把这段当作新版已达长时稳定的证明。

从保存的 step 1227 原生检查点在内存中重放唯一未保存的 step 1228：canonical `τ=2.0663179526496389`，Y 核心降至 `31.998569`，首次触发补充层级搜索。原代码在 `searchEvidence>validate_evidence` 第 84 行要求第一补充阶段结束时累计 330 次 X 试探（70 个初选 + 260 个补充），而密度版本 1/2 的圆滑过渡参数 `[8,16,24,32,40]` 实际只生成 230 个新的第一阶段元组；于是触发 `ipm:AutonomousMeshSearchEvidence`。第 0 阶候选、Y 几何和真实迁移均不是此次异常的原因。原版失败堆栈在 `result/verification/mode_two_long_20260914/reproduce_failure_v1.log`。

修复只对均摊密度参数族把**补充阶段**的最大圆滑过渡从 40 延伸一个半步至 44；第 0 阶的 70 个候选、密度函数、旧策略参数族、节点数和原有质量门不变。这样第一补充阶段重新得到 260 个唯一元组，总阶段计数与冻结契约 `70/260/170` 一致。`serverInterface` 新增两种均摊密度均与注册阶段数逐项相等的回归断言；对同一签名检查点重放未保存步，规划已成功并返回 42 个按节点级登记的候选。修复后实际迁移及短续算需另行核验，不能仅由规划通过推断。

R2.5 目录与 ZIP 保留原值以复现这次停机。修复属于独立的新源码版本；若从 R2.5 checkpoint 续算，需先在隔离目录证明恢复、规划、实际迁移和后续步进，再用新发行包运行，不能在原目录替换正在使用的源码。

已用修复源码在新目录从该 step 1227 原生 checkpoint 实际续算至 `τ=2.2`：step `1227→1323`，自主重布 `13→14`，正常因设定的 `final_time` 结束，末网格仍为 `321×161`，并保存新 checkpoint。报告在 `result/verification/mode_two_long_20260914/fixed_short/report.json`；此短续算只越过首次错误点，尚未证明以后所有层级或强奇异时的稳定性。

末态检查点重新验签通过。新增的第 14 次事务审计本身通过：真实 X/Y 核心 `41.026246/36.799648`、前沿 `24.185146`、最大相邻比 `1.068315/1.038745`、相对峰跳 `1.369e-7`、相对质量缺损 `2.861e-15`；记录在 `result/verification/mode_two_long_20260914/fixed_short_audit_v1.log`。完整 `ipm.verify('all','quick')`（94.7729 秒）与旧数值等价快速套件（5.7372 秒）均通过。
