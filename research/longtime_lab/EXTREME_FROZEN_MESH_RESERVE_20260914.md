# 极端冻结态的同节点网格余量（2026-09-14）

本次使用验签的接受态，不改动任何原轨道：新 C 的 `τ=3.500136`、旧 C version 5 的 `τ=12.400290`，以及更尖锐的旧 C version 4 的 `τ=14.100094/14.200201`。研究脚本 `stress_last_frozen_remesh.m` 对正式候选轴执行原生 `remesh.transfer`、`auditCandidate` 和后续 `flow`；旧 C version 4 的试验是把新密度轴放在旧策略允许的参考节点级上的**反事实原生迁移**，不是声称其旧检查点能切换冻结策略续算。所有原检查点与正在运行的 R2.2 新 C 轨道保持原样。

| 冻结态与目标节点 | 密度 | X/Y 最大相邻比 | 真实核心 X/Y | X 变比负担参与率 | 原生结果 |
| --- | --- | --- | --- | --- | --- |
| 旧 C `τ=12.400290`，`641×321→961×321` | v1，`xPadding=1.10` | `1.044411/1.038809` | `35.152/36.981` | `91.36%` | 审计、flow 通过 |
| 旧 C `τ=14.200201`，`961×321→961×321` | v1，`xPadding=1.10` | `1.049021/1.042217` | `32.889/37.049` | `91.36%` | 审计、flow 通过 |
| 新 C `τ=3.500136`，`641×321→641×321` | 强余量 v2，`xPadding=1.50` | `1.050595/1.026118` | `46.456/36.158` | `87.04%` | 审计、flow 通过 |
| 旧 C `τ=14.100094`，`961×321→961×321` | 强余量 v2 | `1.050411/1.041933` | `45.288/36.256` | `91.36%` | 审计、flow 通过 |
| 旧 C `τ=14.200201`，`961×321→961×321` | 强余量 v2 | `1.059386/1.042217` | `47.767/36.942` | `79.71%` | 审计、flow 通过 |

负担是相邻单元格宽的 `|Δ log h|`；参与率为 `(Σb)^2/(nΣb²)`，全体接口完全均摊时为 100%。强余量版 X 参与率在最尖锐态下降，是因为更多单元落在近乎等宽的核心补丁；其最重 5% 接口承担总负担 6.42%，仍接近完全均摊时的 5%。Y 的参与率约 99.17%。所有格比继续低于原有 1.08 全轴门，没有靠增点或放松质量门提高核心余量。

`sweep_extreme_density_reserve.m` 的**纯几何、未注册参数扫描**说明固定节点数并非在 v1 目标 32 格处耗尽：`τ=14.2` 选 `xPadding=1.5` 时预测 X 约 47.76 格，最大比约 1.05939；新 C `τ=3.5` 预测 X 约 46.45 格，最大比约 1.05059。随后才把 `xPadding=1.50` 注册为独立的 `densityVersion=2`，保留原 v1 与省略字段的策略语义。上述原生旧态报告形成于注册前，但使用同一数值参数；需另用正式 v2 planner 验证新配置选择的一致性。

这些冻结态只证明**当前场可以安全迁移**，没有证明新密度沿实际 PDE 轨道能到 `τ=14` 或强奇异性，也没有证明极限 Profile 的网格收敛。尤其 v1 在 `τ=14.2` 仅比 32 格目标高约 0.89 格，连续寿命余量偏薄；v2 的约 15 格额外余量是为减少后期重网格频率而设置。下一步必须从 `t=0` 以 v2 冻结政策长跑，保留每次自然事务、检查点、停止原因，并在增加节点前审视失效前缀与同物理时刻的加密比较。

正式 v2 接口测试通过；`t=0→τ=.5` 的自动运行有 304 步、6 次自然重布，末态 X/Y 核心 `43.442/36.230`、最大比 `1.049928/1.043198`。从 `τ=.25` 验签 checkpoint 续到 `.30`，重布次数 `1→2`，冻结版本保持 2。正式 v2 在新 C `τ=3.5` 的 planner 选取与先前探索参数逐值一致，原生迁移及 flow 通过。以上仍是短程兼容性和冻结态压力测试。

原始证据：`result/verification/mesh_extreme_20260914/` 的 `*_native_v1.json`、`*_reserve_sweep_v2.json`；早期密度实验见 [BALANCED_MESH_DENSITY_20260914.md](BALANCED_MESH_DENSITY_20260914.md)。
