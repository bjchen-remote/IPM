# IPM 长时间服务器第二发行版修订（R2.2，2026-09-14）

R2.2 **取代 R2/R2.1 作为新算例的推荐包**；旧压缩包保留，只用于其原生检查点的逐值复现。新的服务器默认幅值规范固定以 `(X,Y)=(2,0)` 为中心、半宽 `0.5` 的壁面密度加权 `L2` 值：

```text
R_tau = B + c_omega R,
c_omega = -sum(w_i R(X_i,0) B(X_i,0)) / sum(w_i R(X_i,0)^2).
```

这里的紧支集 `1.5<X<2.5` 与 `X≈1` 的尖峰核心隔开。若末态壁面梯度局部呈 `|X-1|^{-p}`，全域梯度 `q` 次积分在 `pq≥1` 时可能发散，不能用它作不依赖未知幂指数的幅值规范。当前外区密度窗避开局部奇点，因此只要奇性仍局限在该核心附近、外区密度有限，所选积分量就有限；它固定一个外区量，**不保证**每个外区斜率均逐点静止。

四个验签旧轨道冻结态在 `τ=12.3003→14.2002` 间显示 `R(2,0)` 下降 `44.664%`，而 `R_X(X,0)/R(2,0)` 在 `X=1.4,1.6,2,2.4,2.8` 的相对变化均小于 `0.39%`。`τ=14.2002` 的同一原生 RHS 上，新窗给出 `c_omega=+0.00149598426`，旧峰值规则给出 `−0.30419842`；外区样点的瞬时对数斜率约 `−0.00102` 至 `−0.00128`，内区峰节点约 `+0.29806`。从 `t=0` 的服务器配置短跑走了 10 步至 `τ=0.01`，窗值相对漂移 `2.22e−16`；后期验签状态的内存短分支走了 3 步，窗值漂移同量级、`R(2,0)` 变化 `2.05e−10`。短跑没有触发后续自主重网格；另一次有界网格迁移确认冻结参考值保留。

**新规范尚未通过从 `t=0` 到 `τ≈14` 的长程轨道、跨重网格时空加密或大箱检验。**R2.2 可用于启动这些实验，但不是无穷时间 Profile 或最终幂律的认证。旧峰值规范仍可明确选择，用于对照及使用旧包续算。详细推导和证据见 [外点规范研究说明](research/acceleration_lab/OUTER_WALL_DENSITY_GAUGE_X2_20260914.md)。

此前 R2.1 的工程验收仍有效：程序已具备从物理 `t=0` 无需中途人工改网格、自动长时间推进的能力。本机 H8 version 4 主线在用户主动暂停前自主完成 42 次重布、推进到可信 `τ=14.2002012595`；version 5 从 `t=0` 自主完成 37 次重布、推进到可信 `τ=12.4002900296`。两条旧规范轨道在已观测区间均未因网格失败而停机，且都在用户指令下暂停。R2.1 当时只修正能力说明，不改变 R2 数值代码。用法见 [服务器说明](README_SERVER_ZH.md)，网格问题见 [网格建议](MESH_GRID_RECOMMENDATION_ZH.md)。

## 为何在此发布

当前 H8、第 38 次真实网格请求的同源隔离对照表明，过早跨节点级加密已有明显成本门槛：

| 指标 | 正式选择 | 提前跨级增长 | 变化 |
| --- | ---: | ---: | ---: |
| 网格节点 | 205761 | 308481 | +49.92% |
| 最大相邻格宽比 | 1.075623 | 1.060356 | 改善 0.015267 |
| 到下次网格请求的时间 | 0.249941689 | 0.330485837 | +32.23% |
| 单次原 RHS 墙钟时间 | 0.35 s | 0.84 s | 约 2.39 倍，非稳定基准 |

相邻比已经远低于用户上限 2，继续增加近一半节点只换来有限的几何改善；虽然真实重布间隔增加，单次 RHS 更昂贵，粗略折算的单位 canonical 时间 RHS 成本约为 2.35 倍。因此，**就目前这个 H8 请求的成本加权网格比目标而言，继续提前加密已出现边际收益受限**，适合作为阶段性发行点。这是已能稳定自动长跑之后的成本优化取舍，并非程序仍常因网格停机。这个单事件不能证明所有时刻、所有计算箱的收益单调递减，也不能把粗略计时当成性能验收。详细证据见 [跨级成本和续跑](research/longtime_lab/CROSS_LEVEL_MESH_COST_AND_RHS_20260913.md)。

第 42 次正式跨级增长已经自主通过真实迁移，主线验签到 `τ=14.2002012595`；但迁移前后归一化形状本身只跳变 0.01896%，原方程 RHS 给出的墙面形状导数却几乎反向。自动长跑和网格问题得到控制是已经观测到的工程进展；极限 Profile 的数值收敛仍需更大计算箱、空间/时间细化、独立初始网格和长期形状动力学实验。所有本机长跑现已按用户指令暂停，不会随发布包自动启动。

## 使用与恢复边界

- 新算例从物理 `t=0` 使用 `server/profile_settings.m` 和 `server/launch.sh`；默认 `maximumAdjacentGridRatio=2`、自动网格 version 5、周期 checkpoint、较远的运行上限。其他数值安全门仍有效。
- 已暂停的旧轨道必须使用各自冻结的源码与配置恢复：主线 version 4 的最新可信点为 `τ=14.2002012595`、step 19135；version 5 为 `τ=12.4002900296`、step 12890。完整本机文件名可由各运行目录的 `checkpoint_manifest.jsonl` 取得，恢复前用 `server/profile_status.m` 验签。
- R2.2 压缩包包括源码、服务器接口、测试、说明和小型研究证据；**不包括**大型轨道 checkpoint。跨机器续算旧 checkpoint 可能受浮点签名与线程数限制。发布包不声称已得到无穷时间极限、空间收敛或奇异性证明。
