# Directional finite node-family v3 research candidate

The independent implementation and complete contract are in
`result/verification/autonomous_runtime_20260909/node_family_v3_directional_candidate_v1/README.md`.
It is dormant and has no native v3 runtime or resource qualification.

It preserves original policy versions1/2 and the original four reference members, appending
`[3,1],[1,3],[3,2],[2,3]` only for explicitly registered v3. Cap310000, at most3growth transitions,
original per-member70×4 search and3pair budget (24 total), and every old numerical gate are fixed.
Both factor3 axes come directly from immutable original selectedBase, preserve root knots, and do
not require nested factor2 intermediate nodes. Config initialN remains frozen; actualN is a registered
member. The optional initial observer remains originalt0/k8/anchor1,600000 observation nodes, separate
from the actual solver cap. No old checkpoint upgrade is implemented.

Actual noLU tests passed: main11869 and supplement57799 exit0,5 old native CPs and4 old results exact,
original four actual reference members bitwise,8 synthetic directional controller states,41 contract
negatives, plus4 polynomial/telemetry positives and13 audit/early-transfer negatives. Twenty MATLAB
files have no CodeAnalyzer findings. Two cwd-shadowed supplement launches failed before the helper;
raw failures are retained. No LU, native v3 migration/checkpoint/restore or PDE was executed.

The five-member draft and single961×321 resource protocol remain separately preserved. This new
source does not silently include the separately researched hierarchical axis search. Resource and
native original-t0 lifecycle qualification remain necessary before any promotion or long run.
