# R2.4 balanced 密度候选包（2026-09-14）

这一候选包把自动网格 version 5 的**密度分配**作为新算例的显式选择。服务器启动文件默认 `settings.meshDensityVersion=1`；它不增加默认节点数，也不放松最大相邻比 1.08、曲率 0.01、核心/前沿、场迁移等原生安全门。R2.2/R2.3 的发布目录和正在运行的原始 `t=0` 新 C 轨道未改动。旧检查点请用原发布包继续；不能在续算时从旧密度规则切换到本候选。

新 Y 密度使单元宽度在靠近原点处平滑起步，然后按几乎恒定的对数斜率增长，把相邻网格比均摊到外区。X 的既定 70 个候选数不变，但加入较短的平滑过渡。在可信新 C `τ=3.500136`、**同 641×321 节点数**上，正式新候选相邻比 X/Y 为 1.047919/1.026118，原实际网格为 1.049257/1.039181；原生转移审计与后续流场检查通过。旧 C `τ=12.400290` 的同一 961×321 目标节点数上，旧/新正式候选总最大比分别为 1.059659/1.044411；该晚期目标尚未构造原生 LU/迁移。

新模式从物理 `t=0` 跑到 canonical `τ=.5`，277 步、6 次自然重布、正常结束；`τ=.25` 验签检查点续跑到 `.30` 通过。前两次自然 Y 选择仍为旧 Gaussian 候选；新 geometric Y 的正式选择与原生迁移证据来自 `τ=3.5` 冻结态。完整 `ipm.verify('all','quick')` 与旧数值等价套件均通过。**尚未验证**新模式连续长跑到强奇异性、不同箱长/网格/时间步收敛，也不能因此断言无穷时间 Profile 已收敛。

服务器使用见 [README_SERVER_ZH.md](README_SERVER_ZH.md)；详细网格问题、比值分布、约束下界、图与原生审计见 [密度函数实验](research/longtime_lab/BALANCED_MESH_DENSITY_20260914.md)。包内 `result/verification/mesh_density_20260914/` 保存本次精简证据。需要保留旧密度规则的新算例时，在启动前将 `settings.meshDensityVersion` 设为 `0`。
