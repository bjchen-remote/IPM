# R2.7 4:1 高分辨率服务器候选包（2026-09-14）

本包在 R2.6 自动网格与 `(2,0)` 新 C 数值源码上新增**独立的高分辨率入口** [`main.m`](main.m)：默认原始 `t=0` 网格为 `1281×321`（1280×320 单元，严格 4:1），自动 REMESH v5、均摊密度 v2、总节点预算 1,700,000，结果写入发布目录下绝对路径 `runs/profile_H8_4to1_n320/`。4:1 只固定初始单元数，后续自动网格仍可按 X/Y 不同方向增点。原 `server/launch.sh` 仍使用标准 `321×161` 起点；两种入口必须使用不同输出目录。包可复制到服务器任意绝对目录，`main.m` 自行从自身绝对路径确定源码与输出根目录。

若解压在 `/workspace/ipm_long_time_server_r2_7_highres_4to1_candidate_20260914`，直接按绝对路径启动：

```bash
/opt/MATLAB/R2026a/bin/matlab -batch "run('/workspace/ipm_long_time_server_r2_7_highres_4to1_candidate_20260914/main.m')"
```

运行前可在该命令前加 `IPM_PREFLIGHT_ONLY=1`，只核对解析后的绝对路径、初始节点和预算，不启动 PDE。

也可用同包的 `server/launch_high_res.sh`，它会生成真实绝对 `main.m` 路径；设置 `MATLAB_BIN` 指向服务器上的 MATLAB。启动前检查磁盘、空闲内存和系统负载。要改网格规模，只编辑 `main.m` 顶部的 `n` 和 `maximumTotalNodes`，要求 `n` 是 4 的倍数；每个新配置用独立输出目录。续算必须保留数值设置不变，在 `main.m` 中填同一轨道的**绝对**验签 checkpoint 路径。旧二次峰值 C 检查点不能按新 C 续算。

用户记忆中的 `τ≈15` 对应旧 C 长跑的晚期阶段；本机可核实的最长验签接受态实际是 `τ=14.2002012595`、`961×321`，未发现 `τ=15` 验签断点。依据这一极端冻结态，对比 `1025×257` 与 `1281×321` 的正式几何规划：后者最大 X/Y 比 `1.035636/1.042217`、X 核心 46.687 格；前者 `1.046834/1.055349`、X 核心 36.833 格。新 C v2 `τ=3.700010` 冻结场上的同类结果也支持后者。两个根网格的原始解析 `t=0` 无 LU 预检均有合格候选；默认 `1281×321` 登记 8 个方向级别，其中包括双向二倍级别。证据和限制见[选择记录](research/longtime_lab/FOUR_TO_ONE_HIGH_RES_CHOICE_20260914.md)。

本包数值 `+ipm` 与 R2.6 逐文件相同，新增的是入口、资源预设、研究审计和说明。绝对路径 `main.m` 的无求解预检、标准入口 3 步原生短跑、`ipm.verify('all','quick')` 以及 `ipm.verify('equivalence','quick')` 均通过。高分辨率实际 Poisson LU、完整初值、时间推进与长时稳定性尚未在目标服务器验证；1,700,000 是**上限**，不是内存保证。R2.6 低节点新 C 轨道还在运行，不能把旧 C 的 `τ=14.2` 当作此高分辨率新 C 版本已达时间。
