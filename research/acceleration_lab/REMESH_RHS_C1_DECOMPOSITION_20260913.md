# 第38、39次重网格：原 RHS、C1 峰值与形状导数分解

在 H8 原始 `t=0` version-4 主轨道的两次真实请求处，从请求前验签 checkpoint 重放至准确来源步，原生迁移后审计逐值匹配后续验签事务。迁移前后使用各自的**原生接受态 RHS 缓存**；在迁移前连续峰值和 0.9 核宽定义的**同一内层坐标**上，按各自维护的 Dx/Dy 及 C1 Hermite 观测 `rho`、`Omega=rho*Dx'`、`rho_tau`、`Omega_tau=rho_tau*Dx'`。尺度元组与 canonical/physical 时间相同。没有调用新 Poisson 求解、修改 C、推进替代轨道或写原生 checkpoint。

| 事件 | `rho` 墙面相对 L2 差 | `Omega` 差 | `rho_tau` 差 | `Omega_tau` 差 | 归一化墙面形状导数差/前值 |
|---|---:|---:|---:|---:|---:|
| 第38次 | 2.63e-7 | 3.97e-5 | 0.805% | 1.400% | 78.9% |
| 第39次 | 2.45e-7 | 4.00e-5 | 2.440% | 2.545% | 1798% |

第39次迁移前形状导数范数仅 0.349，迁移后 6.592，所以按迁移前范数计的 1798% 具有小分母放大；绝对范数也明确显示大差异。原形状与场迁移很近，却没有获得导数稳定性。上表的内层采样 L2 不是空间收敛阶或连续 PDE 的误差界，哪一个网格更准确仍未知。原形状导数的 ε=`1e-5/5e-6` 半步复核在第38次低于 0.07%、第39次迁移前约 0.178%、迁移后低于 0.006%，远小于导数跳变（见[前一分段报告](ORIGINAL_T0_TAU13_SHAPE_AND_REMESH_20260913.md)）。

单独读出连续 C1 峰值的实际 `PPrime/P` 和峰/宽几何率，再列出**假设只改幅度规范**所需的瞬时 `c_omega^(C1)=c_omega^(old)-PPrime/P`；此式未回填 RHS，也不是一次 C1 轨道测试。

| 事件 | 原 `c_omega` 前→后 | 假设 C1 `c_omega` 前→后 | C1 峰位率/墙宽 前→后 | C1 墙宽对数率 前→后 | C1 竖宽对数率 前→后 |
|---|---:|---:|---:|---:|---:|
| 第38次 | −.309140→−.313811 | −.308923→−.312806 | .911→1.275 | −.494865→−.494904 | −1.254→−.686 |
| 第39次 | −.307541→−.321705 | −.305096→−.318215 | 1.388→.273 | −.461008→−.599632 | −.594→−2.298 |

两侧的 C1 峰值与连接根均有效；第38、39次的假设 C1 幅度率仅使 `c_omega` 跳变分别缩小约 17%/7%，**不能单独修复**大形状导数差。对墙面归一化剖面 `q(ξ)=Omega(a+wξ)/P`，形式上的切向包含原 `Omega_tau/P`、峰位运动 `a' Omega_x/P`、宽度运动 `w'ξ Omega_x/P` 和幅度项 `−(P'/P)q`。因此小的原 `Omega_tau` 相对差仍可能经 `a'/w`、`w'/w` 和峰局部曲率放大。表中的峰位与竖宽率差给出具体放大通道，但未做逐项范数归因，不能说由某一项独占。

独立完整数据：[第38次](evidence/native_rhs_c_decomposition_tx38_20260913.json)、[第39次](evidence/native_rhs_c_decomposition_tx39_20260913.json)；复现脚本为 `audit_native_remesh_rhs_c_decomposition.m`。原先按相邻 Profile 差值拟合一个慢收敛倍率的做法缺少导数与几何率稳定性。下一轮应在相同请求场测试同成本与增点备选网格的 RHS/形状切向，并对较大物理箱、空间节点和时间步做匹配比较。新网格评分可以先影子记录导数差，不能直接加一个未经误差验证的硬停机门，以免把连续长跑截断。强奇异性和无穷时间 Profile 均尚未证实。
