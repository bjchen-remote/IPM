# R2.3 候选版：外区截断收敛诊断

R2.3 保留 R2.2 的 `(2,0)` 外壁面密度窗幅值规范、version-5 自动网格、
最大相邻格比 `2` 选项及原有长跑接口。新增的
`server/analyze_outer_profile.m` 只读取验签的接受态 checkpoint，
在固定重标定坐标计算避开 `(1,0)` 半圆核的 `R_{X\tau}` 截断
`L^1/L^2/L^4/L^\infty` 范数，以及相邻剖面在公共观测格上的变化。
原生 RHS 用于单态导数，不用两个稀疏时间样本相减来估计导数。

```matlab
addpath('server')
report = analyze_outer_profile('/data/ipm/run01');
```

默认在运行目录的 `analysis/` 下写新的时间戳 JSON 文件；第三个参数可限制
最近 checkpoint 数量，第二个参数可显式指定输出文件。它不会继续 PDE、
改写 checkpoint 或改变当前 C。数学定义和解释见
`research/acceleration_lab/OUTER_CUTOFF_MIXED_DERIVATIVE_20260914.md`。
这里的 `R_{X\tau}` 是重标定固定坐标下的混合导数，不等于移动物理位置的
`\rho_{xt}`；判断重标定 Profile 外区静止应首先用前者。

R2.2 的连续纵向核心影子观测器依赖旧二次峰幅值规范专有的峰位置；
新 C 下该位置为 NaN，实验的每个接受步均出现被隔离的
`ipm:ContinuousVerticalCoreInput` 观测失败。R2.3 在专有位置不存在时使用
已经计算的 `trackedPeakX`，恢复影子观测；旧二次峰规范仍走原分支。
该观察器不参与正式网格请求、迁移、幅值率或终止安全门。

初步筛选：旧峰值 C 的四个验签冻结态各自用 `(2,0)` 密度窗归一化后，
排除半径 `0.2` 的 bulk `L²` 外区剖面相对增量为
`0.00523/0.00676/0.00157`，时间间隔不等。这只是反事实形状筛选。
独立 R2.2 新 C 从原始 `t=0` 运行到 `\tau=3.00041744` 时已有 21 次
自主重网格，最新为 `641×161`；半径 `0.2` 的 bulk `L²`
`R_{X\tau}` 在 `\tau≈1.5/2/2.5/3` 为
`0.12154/0.10165/0.09278/0.09822`。壁面导数虽下降，bulk 高 `p`
仍可能增长，不能声称外区收敛。该 H8 轨道的远边界指标也超过
`0.01` 告警阈值，最终 Profile 必须做大箱及时空网格比较。

这是一版诊断与观测器修复，长时新 C 稳定性、强奇异性及无穷时间
极限尚未认证。运行中的 R2.2 实验继续使用其原封不动的发布源码，
不因本版更新而更改轨道。
