# 第一象限与 level-set 直接重网格：架构记录

日期：2026-09-14。实现位于 `ipm_grid_v6/`，保留 `ipm.solve(opts)` 一个求解入口。旧发布目录、旧检查点及服务器正在运行的冻结源码未修改。本记录说明已实现的路径；短程等价不等于长时间收敛。

导航：[结构总览](STRUCTURE.md) · [+ipm 包索引](+ipm/README.md) ·
[remesh 包说明](+ipm/+remesh/INTERNAL.md) · [验证说明](tests/README.md)。

## 决策与范围

`opts.quadrantOnly=true` 显式选择 `0≤X≤H, 0≤Y≤Ymax`，要求 `xlim=[0,H]` 和 `symmetryMode='double_odd_omega'`。`nx` 是实际存储的正半轴节点数，不再用奇数全域 `nx` 表示一半有效节点。场、运行算子、快照、结果和原生 checkpoint 都按 `ny×nx` 及非负 `x` 记录。省略该选项时旧配置和数值路径保持不变。

当前第一象限只准入各向同性 `high_order / weno5_fd / ssprk54 / high_order` 数值元组。可沿用 `transport_anchor` 和 `(2,0)` 外壁面密度窗等动态规范。旧 `autonomousMesh` v1–v5 的对称轴/节点族契约不适用，配置解析明确拒绝与第一象限混用；新路径使用同节点数直接重网格。自动增加节点数、各向异性象限路径和百万节点长跑尚未实现或验收。大网格应在启动时直接给出所需 `nx,ny`。

网格质量量用于 REMESH 触发、新网格事务验收和事后可信区间分析，不作为 PDE 主循环的终止条件。历史停机阈值仅在运行期新网格已成功接受后评估；超阈值事件输出 `WARNING`、持久化到 `metadata.remeshWarnings`，但不回滚网格、不改写 `stopReason`。REMESH 提案拒绝、几何构造或迁移异常同样保留当前接受网格并发布去重警告。服务器 v3 把 canonical/physical 时间、接受步数和 REMESH 次数上限全部设为 `Inf`，唯一配置的正常终止条件是物理 `max|rho_x|>=1000`；该量在每步检查、每次输出播报。只有非有限算术或时间步下溢到机器零这类无法形成下一接受态的数值故障仍会退出并保存最后有限态。

## 数学与存储契约

关于 `X=0` 奇偶延拓：`R` 偶，`ω=R_X` 奇，`ψ` 奇，`U₁=-ψ_Y` 奇，`U₂=ψ_X` 偶。因此 `ω(0,Y)=ψ(0,Y)=U₁(0,Y)=0`，但 `U₂(0,Y)` 可以非零。`Y=0` 仍要求 `U₂(X,0)=0`。Poisson 只在正半轴内部装配；`X=0` 和远端 `X=H` 是 Dirichlet 迹，Green 镜像公式只积分正象限源。负 `X` 的二维密度、流函数、速度、权重和 Poisson 未知数不再分配。

`mesh.quadrantDerivative` 每次只用有限宽度的局部虚拟奇偶模板，并直接折叠成 `nx×nx` 稀疏矩阵；没有镜像场或整条负半轴网格。偶模板给出 `R_X`，奇模板给出 `ψ_X`、`U₁_X`、`ψ_XX`。`mesh.quadrantQuadrature` 只积 `[0,H]`，权重对应旧全域积分的正半域。Green 源超出压缩阈值时，沿用旧全域的概念分箱编号，但只压缩实际正半轴源。WENO 在 `X=0` 使用局部反射 ghost；分裂通量的正则化尺度与概念全域一致。`Y` 和远端边界保持原闭合。
非均匀重网格的逻辑映射 `X(ξ)` 也是奇函数，其度量 `dX/dξ` 用奇模板求导；这样聚焦网格在对称轴附近仍与旧全域模板一致。

一个 `1025×1025` 第一象限场含 `1,050,625` 个节点；同正半轴分辨率的旧全域场是 `2049×1025`、`2,100,225` 节点，标量场节点约减少 49.98%。Poisson 内部未知数从 `(2049-2)×(1025-2)` 降为 `(1025-2)^2`。这不是整段运行已测得两倍加速。稀疏直接 LU 的填充、峰值内存与耗时须在服务器实测；迁移时旧/新分解还可能短暂共存，百万节点前需要内存预飞或进一步实现释放旧 LU 后的提交事务。

## 一次直接重网格事务

调用链：既有 `flow` 的横向/纵向 90% 核心单元数预筛 → `remesh.directLevelSetAdapt` 重测 level set → 一对轴 → 原生 `transfer` → 网格质量/守恒/峰值/值域门 → 重算新 `flow`。若用户另设最高观察层，跳过预筛而直接重测 90% level set；两者共用目标及 80% 阈值。象限触发不使用旧综合 `safetyFactor`，但要求核心观测有限；无正峰的物理态不进入反演。初始化可先对解析初值重网格，再重新采样解析初值。运行期只读已接受场；格点计数不进入 `c_l,c_ω,c_r`。

唯一几何输入是 90% 正壁面峰核心区间、峰左侧前沿区间、同峰列 90% 纵向宽度。由这些区间计算平台中心、左右跨度与间距：`h=min(coreWidth/targetX, frontWidth/targetFront)/1.15`。`h` 与边界位置一次确定平台格数、左右比例和固定 `X=1` 锚点；圆滑对数格宽接到 `X=0,H`。横轴细区外的左右跨度 `L,R` 按 `log(1+L/h):log(1+R/h)` 一次分配剩余单元，投影到两侧均能装下最小格宽和圆滑过渡的整数区间，仅求解两条标量斜率。纵轴用纵宽和目标格数求解一个单调标量方程。不存在左右单元枚举或二维候选搜索；标量二分不是猜场解。

小网格目标由既有规范目标裁到节点预算，触发阈值为目标格数的 80%。格比上限是 `min(remeshMaximumCellRatio,1.20)`；核心/前沿预测、求积正性、模板条件及真实迁移分别验收。几何不够时明确返回原网格。第一象限每次最多 1 对几何轴、1 次原生迁移；旧 v5 的 `70/260/170` 试探只属于旧全域模式。同节点数反复失去容量、需要增加节点时，目前会显式保留旧网格并继续演化，不会悄悄突破预算。

横轴分配由逐个整数试探改为一次整数投影，`splitEvaluations=1` 可在构造信息中核对。
象限轴构造入口直接接收实际正轴节点数，概念全域节点数仅是诊断信息；
目标核心数与 80% 触发单元数由同一函数返回，预筛和重测使用同一门槛。
若请求细格宽超过固定节点数与左右跨度允许的最大值，构造器在连续平衡分配附近
只比较相邻两个整数，解析得到最大容量并一次下调细格宽；原先这种可行性不足会
抛出断言，现在不再终止 PDE。正常未限幅轴保持既有浮点运算顺序和坐标逐位不变。
固定次数的三条标量二分仍各自遍历一维节点，因此新象限横轴构造随节点数近似线性；
这不代表二维原生迁移和 Poisson 分解的总耗时也线性。1025 个正半轴节点的纯几何
构造已进入回归测试；1024² 原生求解的内存/耗时仍待服务器预飞。

## 模块边界

| 模块 | 第一象限职责 | 保持的合同 |
|---|---|---|
| [config](+ipm/+config/INTERNAL.md) | 可选 `quadrantOnly`、正域与数值元组验证 | schema 4；旧配置无新字段 |
| [mesh](+ipm/+mesh/INTERNAL.md) | 非负轴、奇偶导数、半域积分与 Poisson | `ny×nx` 同网格算子 |
| [field](+ipm/+field/INTERNAL.md) | 正源 Green、反射 WENO、对称轴速度 | 原 Green/输运接口 |
| [diagnostics](+ipm/+diagnostics/INTERNAL.md) | 正半轴 level set；远边界仅含 `X=H,Y=Ymax` | 峰、宽度与 REMESH 后告警 |
| [remesh](+ipm/+remesh/INTERNAL.md) | 单次直接几何及原生迁移 | 失败回退、质量和值域门 |
| [output](+ipm/+output/INTERNAL.md) | 非负轴/场/checkpoint | result v2、checkpoint v4 |

## 实测证据与限制

- `129×65` 全域和 `65×65` 第一象限的 `k=8` 动态初态：Green、Poisson、速度、规范率与完整 RHS 对照；RHS 最大绝对差 `3.9774e-14`，`X=0` 的 `ω,ψ,U₁` 精确为零。此比较已进入 Green 源压缩分支。
- `65×33` 直接重网格：目标核心 `12/10`，旧核心约 `7.7066/7.6739`，一次提案/迁移，预测新核心约 `13.6377/11.5000`，守恒/峰值/范围门及新流场有限性检查通过。4 步短程求解后仍是 `65×33` 非负轴，重网格计数 1。
- 将上述真实聚焦后的非均匀正轴临时构造成旧全域对照算子（仅测试）：度量、源、流函数、速度和 WENO 输运的最大绝对差 `1.2434e-14`。运行路径不构造该镜像全域。
- 原生 schema-v4 checkpoint 验签读取并恢复到同一正轴；旧全域 6 个物理算例、3 个迁移算例与保留原版逐值一致。`ipm.verify('quadrant')`、baseline 已通过。全套验证的最终命令结果以 `CHANGELOG.md` 记录为准。
- 未验证：1024² 的 LU 峰值内存和耗时、长期自适应容量、网格/时间/箱长独立收敛、触发后长期规范稳定性。本路径是可运行可检查的第一象限架构与短程数值实现，不是奇性结论或服务器百万点性能认证。

## 使用

短例见 `examples/quadrant_level_set.m`；验证运行 `ipm.verify('quadrant')`。
服务器新发行包根目录直接含可复制的 `main.m`：源码固定指向
`/data/user/hd58131/ipm/ipm_long_time_server_20260915_v3`，仅修改旧绝对路径末尾版本号。
把主文件复制到作业目录，以**复制件绝对路径**
运行；仅 `jobRoot` 从自身路径获得，输出进入作业目录。打包和预飞见
[第一象限发行入口](server/README_QUADRANT_ABSOLUTE.md)。不改用旧全域发布包。
服务器可直接将 `nx,ny` 设为例如 `1025,1025`，但须先做单步和 LU 内存预飞。
旧全域 checkpoint 不得改写 `xlim` 后作为象限续算；冻结配置、网格与规范始终按原生检查点恢复。
