# 原始 `t=0` H8 轨道的内层形状与加速目标（2026-09-13）

四个同一 case 的 schema-4 原生 checkpoint（step 1650、2130、2741、2964）已逐个通过 `ipm.output.readCheckpoint` 和完整可信历史检查。使用既有只读 `analyze_profile_series` 对配对的 `rho/x/y` 重新应用维护的 7 点导数；未建 Poisson LU、未推进 PDE、未改变 C 或原始轨道。数值与图在 `result/verification/autonomous_runtime_20260913/H8_profile_tau4_6_v1/`。

| canonical τ | 物理 t | 节点 | 物理峰 `rho_x` | 重标度墙内核宽度 | 重标度竖内核宽度 | 物理峰位置 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 4.0017345 | 1.9143698 | 321×161 | 4.0633376 | 0.0395183 | 0.0105999 | 0.2988046 |
| 5.0004318 | 2.0514798 | 641×161 | 6.2409678 | 0.0212586 | 0.00504582 | 0.2598746 |
| 6.0010030 | 2.1413120 | 641×321 | 9.4554119 | 0.0112135 | 0.00293474 | 0.2364482 |
| 6.3008925 | 2.1618138 | 641×321 | 10.6797157 | 0.00933707 | 0.00238436 | 0.2311407 |

墙内核宽度在三个相邻 τ 区间的对数斜率为 `−0.62081, −0.63928, −0.61063`；从首帧到末帧缩小 4.23 倍，竖内核缩小 4.45 倍。当前精确 transport-anchor / quadratic-peak C 规范保持重标度峰值约 `0.6865`，却没有固定内核宽度。因此这段数据不支持把当前重标度密度当成已接近非零光滑静止解，直接最小化 `F(rho)=0` 的 Newton 方案在这里没有合格目标。它也不能证明晚期不存在别的定态或非光滑极限。

把每帧按自己的峰位置、严格 0.9 宽度和峰值归一后，在固定内层窗口上，相邻墙形状的相对 L2 差分别为 `1.560%, 3.643%, 1.194%`；竖向形状为 `0.140%, 0.198%, 0.0524%`。墙形状差没有单调下降，且 τ≈5→6 跨过 Y 方向增点，不能用这四帧拟合可信的无限时间外推。竖向曲线接近也只是在逐帧移动和缩放后的形状比较，不代表原规范中的收敛。

加速研究下一步应先在更晚、相同节点级的连续原生帧上预登记内层观察窗和留出帧，比较形状预测与原生未来帧，并保留真实的峰位、峰幅、宽度与全域残差。任何外推或 Newton/Anderson 候选均是独立研究场，不能写成原始 PDE 的接受步。H8 的远边界指标已超 `0.01` 告警阈值；最终 Profile 还必须有匹配物理时间的更大箱、空间和时间步误差检查。

## 固定公式的第一次未来帧测试

`test_original_zero_inner_forecast` 仅用前三帧的逐帧归一化形状预测 τ=6.300892 的第四帧；没有输入第四帧去拟合参数。固定比较是保持 τ≈6.001 的形状、按最后一段斜率线性外推，以及从两段训练增量最小二乘求单模态衰减因子的 rank-1 外推。若因子非正，rank-1 按预设条件拒绝，不临时改模型。

| 内层形状 | 保持最后帧相对 L2 | 线性外推相对 L2 | 训练增量余弦 | rank-1 因子 |
| --- | ---: | ---: | ---: | ---: |
| 墙 | 0.0119392 | 0.0226225 | −0.82462 | −1.95438（拒绝） |
| 竖 | 0.00052531 | 0.00107028 | −0.93891 | −1.32533（拒绝） |

线性外推在两条曲线上都比直接保留最后帧差约两倍；正的单衰减模态条件也不成立。这否定了**这个训练窗和固定模型**的加速建议，不能推出所有晚期窗口都不可加速。训练帧跨过两次方向增点，H8 还有远边界告警；下一次研究必须改用较晚、同节点级的预留训练/未来窗口，并同时检查完整物理场和独立空间误差。原始轨道未被预测修改；结果在 `result/verification/autonomous_runtime_20260913/H8_inner_shape_holdout_v1/report.json`，脚本静态检查与实际测试均通过。

## 相同原生网格的短期留出检验

τ=`6.5000615,6.6000525,6.7000655,6.8012445` 四个原生 checkpoint 均是 `641×321`、累计重布 23 次；四对 X/Y 轴逐值相同，且完整 checkpoint 与历史已由 `analyze_profile_series` 校验。相同固定公式以前三帧预测第四帧：

| 内层形状 | 保持最后帧相对 L2 | 线性外推相对 L2 | 单正模态相对 L2 | 训练增量因子 |
| --- | ---: | ---: | ---: | ---: |
| 墙 | 0.00247082 | **0.000378289** | 0.000411996 | 0.971662 |
| 竖 | **0.00123720** | 0.00149925 | 0.00166987 | 1.63261（增长） |

墙短期形状误差相对保持最后帧减小约 6.53 倍；竖向不改进，且其正模态是增长而非收敛。这是 τ≈0.1 的**形状**预测，不预报未来峰值、宽度、位置或物理场，不足以取得无穷时间 Profile。结果在 `result/verification/autonomous_runtime_20260913/H8_inner_shape_holdout_65_68_v1/report.json`；脚本 `test_original_zero_inner_forecast.m` 的 Code Analyzer 为零告警。

为测试更长的外推距离，训练帧 `6.5/6.6/6.7` 的墙、竖形状及增量已在 2026-09-13 06:33:05 UTC 冻结到 `result/verification/autonomous_runtime_20260913/H8_inner_shape_future_frozen_v1/registration.mat`，在主轨道仅到 τ≈6.96 时完成。预登记的未来窗是 `[7.15,7.25]` 和 `[7.45,7.55]`，各取窗内最早的原生接受 checkpoint；模型与训练帧不再重拟合。冻结 MAT 的 SHA256 为 `a642b75fac86718b2bf448a777c30f4de1ae7a3f1f5e1940346168a83892376e`。无论未来预测好坏，H8 的计算域告警仍要求独立大箱验证。

第一个未来窗的最早原生帧为 step 3729、`τ=7.20000264392`；它仍是 `641×321`，但累计重布已从训练时的 23 增至 24，故标记为跨一次同节点重布。原生 checkpoint 与完整历史通过验证；冻结训练时刻及两条 rank-1 因子与实际评分逐值相等，冻结 MAT 的 SHA256 复核不变。

| τ≈7.2 内层形状 | 保持训练末帧相对 L2 | 线性外推相对 L2 | rank-1 相对 L2 |
| --- | ---: | ---: | ---: |
| 墙 | 0.00881302 | 0.00428101 | **0.00370025** |
| 竖 | **0.000688632** | 0.00202591 | 0.00824626 |

冻结单模态墙形状预测比保持末帧误差小 2.38 倍，优势相对 τ≈6.8 的短程测试已减弱；竖向单模态误差是保持末帧的 11.97 倍，且训练因子 `1.6326>1` 原本就是增长模态。此结果只支持一个局部墙形状的短程/中程预测，不支持完整二维 Profile、物理场、无限时间极限或对原始 PDE 的加速推进。第二个未来窗仍须按已冻结注册独立评分。结果位于 `result/verification/autonomous_runtime_20260913/H8_frozen_inner_holdout_tau72_v1/report.json`。
