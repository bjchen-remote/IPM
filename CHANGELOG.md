# 变更记录

## 2026-09-15：level-set 驱动的 REMESH 重试

- 服务器大版本升为 `ipm_long_time_server_20260915_v4`。
- REMESH 失败后冻结 90% 核心中心、横向核心宽度和纵向核心宽度；只有几何累计变化
  达到一个目标单元才重试，不使用固定步数/时间冷却，也不再逐步重复构轴和迁移。
- 将 `trackFeatures` 已计算的全局 `max|R_X|` 缓存在 `flow.rhoXInf`，终止门和输出
  复用该值，删除每个接受步的重复二维扫描。
- 同一 65×33、1172 步无限时域探针从 20.211 s 降至 18.552 s，快 8.2%；
  失败后的完整 REMESH 尝试为 11 次。数值末态和终止步保持不变。

## 2026-09-15：`max|rho_x|=1000` 长时间服务器版

- 服务器大版本升为 `ipm_long_time_server_20260915_v3`；canonical/physical 时间、
  接受步数和 REMESH 次数上限均设为 `Inf`，最小时间步降到 `realmin`。
- 新增唯一正常终止门 `rhoXStop=1000`，每个接受步检查同一物理
  `max|rho_x|`，每次控制台播报和最终摘要均显示该值。
- 运行期和解析初始 REMESH 的提案拒绝、构造或迁移异常改为保留当前网格、持久化
  去重警告并继续；非有限算术等无法形成下一接受态的故障仍保存最后有限态后退出。
- checkpoint 恢复允许只覆盖 `rhoXStop`，便于旧 v2 原生 checkpoint 进入新终止策略。

## 2026-09-15：生产仓库精简

- 删除 601 个历史研究文件、旧 R2.3--R2.7 发布说明、旧全域服务器入口及脚本。
- 删除三个非生产示例和旧源码映射清单；布局测试改为直接验证当前包入口和运行依赖。
- 已跟踪文件从 808 个降至 184 个；当前分支只保留求解器、测试、第一象限示例、服务器最小发布入口和互链架构文档。
- 服务器大版本升为 `ipm_long_time_server_20260915_v2`，入口设置 `maximumSteps=Inf`。最小发行包为 144 个文件、1.1 MB（ZIP 296 KB），包内与 ZIP 校验通过。
- 删除内容仍保存在 Git 提交 `3b58d93` 中；本地 `runs/` 和 `result/` 未删除。

## 2026-09-15：REMESH 后非终止质量警告

- 保留 REMESH 触发、网格构造、原生迁移与事务验收。
- 梯度、值域、壁面振荡、相邻格比和核心分辨率阈值仅在运行期新网格接受后检查。
- 越界事件写入 `metadata.remeshWarnings` 和 checkpoint，并打印 `WARNING`；不回滚、
  不改写 `stopReason`、不终止 PDE。诊断自身异常也降级为警告。
- MATLAB R2026a：`quadrant`、`baseline`、`all/quick` 通过；变更文件 Code Analyzer 0 条。

## 2026-09-15：第一象限直接重网格

- 运行场、Poisson 未知数、结果和 checkpoint 只保存 `X>=0, Y>=0`。
- 90% level-set 边界和尺度一次确定一对网格轴；不使用旧多候选搜索。
- 修复固定节点预算下 rounded-log 分割不可行时的断言退出，改为解析容量限幅或原网格回退。
- 支持 `maxSteps=Inf`，网格质量不再作为主循环硬停机条件。
